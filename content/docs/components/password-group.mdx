---
title: Password group
description: A password input with a built-in show and hide visibility toggle.
---

The `.password-group` is an input group whose trailing addon is a ghost button that toggles
the field's visibility — wired in markup, with no JavaScript.

## Default

<Preview src="password-group/default" />
