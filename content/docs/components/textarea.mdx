---
title: Textarea
description: A multi-line text field on the shared field foundation.
---

The `.textarea` shares the `--field-*` tokens with the other form controls, including
`:user-invalid` validation styling. Use the native `rows` attribute (and `field-sizing` where
supported) to control its height.

## Default

<Preview src="textarea/default" />
