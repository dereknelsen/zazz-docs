---
title: Select
description: A native select restyled with the customizable select API.
---

The `.select` restyles a native `<select>` using `appearance: base-select`, so the dropdown
picker matches the rest of the system where supported — and falls back to the native control
otherwise (gated behind `@supports`).

## Default

<Preview src="select/default" />
