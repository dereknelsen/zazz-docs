---
title: Slider
description: A styled, accessible native range input.
---

The slider is a restyled native `<input type="range">` — accessible and keyboard-operable out
of the box. Use the standard `min`, `max`, `step`, and `value` attributes.

## Default

<Preview src="slider/default" />
