# Zazz component API index

The stable **API surface** only — selector, `data-*` attributes and their accepted values,
what powers the component, and where the canonical example lives. **No example markup is
copied here** (that would duplicate the single source). To author a component, fetch the real
markup from the docs page or read `zazz/components/{name}/*.html` in-repo, then adapt it.

Variants/sizes follow the shadcn-style pattern: a `data-*` attribute reassigns the
component's local tokens (see `zazz/styles/_button.css`). The **default** variant is the
_absence_ of the attribute.

| Component       | Selector(s)                                                                                                                                     | `data-*` (accepted values)                                                                                                | Powered by                                                                                                              | Docs                               |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| Button          | `.button`                                                                                                                                       | `data-variant`: primary, muted, ghost, destructive, link · `data-size`: sm, icon, icon-sm                                 | CSS only                                                                                                                | `/docs/components/button`          |
| Badge           | `.badge`                                                                                                                                        | `data-variant`: primary, muted, ghost, link · `data-size`: icon                                                           | CSS only                                                                                                                | `/docs/components/badge`           |
| Tooltip         | `.tooltip` › `.tooltip__content[popover="hint"]` (+ `.tooltip__arrow`)                                                                          | `data-side`: top, bottom, left, right · `data-align`: start, center, end                                                  | Interest Invokers (`interestfor`) + Popover + anchor positioning · polyfills: invokers, popover                         | `/docs/components/tooltip`         |
| Dialog          | `<dialog class="dialog">` › `.dialog__content/__header/__body/__footer`                                                                         | `data-size`: article, container, screen                                                                                   | Invoker Commands (`command="show-modal"`/`"close"`, `commandfor`) + native `<dialog>` + `closedby` · polyfill: invokers | `/docs/components/dialog`          |
| Dropdown        | `.dropdown` › `.dropdown__popover[popover="auto"]`; trigger `popovertarget`                                                                     | `data-side` · `data-align`: start, center, end                                                                            | Popover API + anchor positioning · polyfill: popover                                                                    | `/docs/components/dropdown`        |
| Navigation menu | `.navigation-menu` › `.navigation-menu__popover` (+ `__viewport`, `__featured`); triggers `popovertarget`                                       | `data-align`: center, end · `data-variant`: submenu · `data-size`: root, container, screen · `data-animation`: slide-down | Popover API + anchor positioning                                                                                        | `/docs/components/navigation-menu` |
| Accordion       | `.accordion` › native `<details>`/`<summary>`                                                                                                   | —                                                                                                                         | Native disclosure (`::details-content` transition)                                                                      | `/docs/components/accordion`       |
| Tabs            | `.tabs` › `.tabs__list[role=tablist]`, `.tabs__indicator`, `.tabs__label`, grouped `input[type=radio][role=tab]`, `.tabs__panel[role=tabpanel]` | `data-direction`: vertical (horizontal is default)                                                                        | CSS only (driven by grouped radios — panel order must match radio order)                                                | `/docs/components/tabs`            |
| Card            | `.card` › `.card__figure/__image/__content/__body/__title/__description/__footer`                                                               | —                                                                                                                         | **Composition pattern** — BEM hooks have no dedicated CSS yet; build from `--card`/`--border`/`--radius-lg` + utilities | `/docs/components/card`            |
| Avatar          | utility composition: `grid grid-area-pile size-* border rounded-full overflow-clip` › `<img>` + text fallback                                   | —                                                                                                                         | **Composition pattern** — no `.avatar` class; pure utilities                                                            | `/docs/components/avatar`          |
| Prose           | `.prose` (in `_typography.css`)                                                                                                                 | —                                                                                                                         | Long-form content wrapper — styles headings/paragraphs/lists/figures                                                    | `/docs/components/prose`           |
| Lightbox        | `.lightbox` › `__gallery/__stage/__slide/__thumbnails/__thumbnail/__dialog` + `[data-embla]` hooks + `<dialog>`                                 | Embla hooks (see `apis.md`) · `data-embla-start` on triggers · `command="show-modal"`/`commandfor`                        | **Zazz JS** (`embla.js` + Embla CDN) + Dialog + Invoker Commands                                                        | `/docs/components/lightbox`        |
| Slider          | `<input type="range">` (styled in `_reset.css`)                                                                                                 | —                                                                                                                         | CSS only                                                                                                                | `/docs/components/slider`          |
| Switch          | `<input type="checkbox" role="switch">` (`_switch.css`)                                                                                         | —                                                                                                                         | CSS only (restyled native input)                                                                                        | `/docs/components/switch`          |
| Input           | `.input` (`_input.css`, shares `--field-*`)                                                                                                     | — (type-driven)                                                                                                           | CSS only · validation via `:user-invalid`                                                                               | `/docs/components/input`           |
| Input group     | `.input-group` › `.input-group__addon` + `.input`                                                                                               | `data-align` on `__addon`: inline-start, inline-end, block-start, block-end                                               | CSS only                                                                                                                | `/docs/components/input-group`     |
| Password group  | `.password-group` › `.password-group__addon` (toggle = `.button[data-variant="ghost"]`)                                                         | `data-align` on `__addon` (same values)                                                                                   | CSS only (visibility toggle in markup)                                                                                  | `/docs/components/password-group`  |
| Textarea        | `.textarea` (`_textarea.css`, `--field-*`)                                                                                                      | —                                                                                                                         | CSS only · `:user-invalid`                                                                                              | `/docs/components/textarea`        |
| Select          | `.select` (`_select.css`)                                                                                                                       | —                                                                                                                         | Native `appearance: base-select`, `@supports`-gated with fallback                                                       | `/docs/components/select`          |
| Checkbox        | `<input type="checkbox">` (`_fields.css`, `_reset.css`)                                                                                         | —                                                                                                                         | CSS only (restyled native input)                                                                                        | `/docs/components/checkbox`        |
| Radio           | `.radio` / `.radio-group` (`_radio.css`, `--field-*`)                                                                                           | —                                                                                                                         | CSS only                                                                                                                | `/docs/components/radio`           |

## Forms

All field types share `--field-*` tokens (`_fields.css`, the Tier-3 owner). Wrap a control in
`.field` (supports `data-direction="horizontal"`) for the label / control / hint / error
layout. Validation surfaces via `:user-invalid` — **after** blur/submit, never while typing.

## Behaviors (not components — see `apis.md`)

- **`[popover]` (base)** — `data-side`/`data-align` positioning powers tooltip, dropdown, and
  navigation-menu; dark "inverted" popovers come from a container `style()` query.
- **Mobile menu** — `.mobile-menu` composes `.dialog[data-size="screen"]` + `.accordion`;
  `data-animation="slide-right"`.
- **Reveal** (`[data-reveal]` / `[data-reveal-each]`) and **Embla** (`[data-embla]`) are
  data-attribute-driven JS behaviors. Their full attribute catalogs are in `apis.md`.

> The `zazz/components/*.html` examples may contain stale attribute values that have no
> matching CSS (seen: `data-variant="outline"` on a button, a `data-size="large"` mention in
> a dialog doc-string). Trust this index + the `_*.css` selectors for the real API; use real
> example markup for structure.
